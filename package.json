{
  "name": "docgit",
  "private": true,
  "description": "Git-style version control for office documents — version tree, branching, and trustworthy diffs for Word, Excel, PowerPoint, La Suite Docs and Grist.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "auditConfigRationale": {
    "_note": "Electron 42 + fast-xml-parser 5.9 (#149) resolved the shipped-runtime advisories. The remaining waivers are all build-only packaging tooling (electron-builder transitives) that never ships in the app.",
    "GHSA-hmw2-7cc7-3qxx": "form-data: build-only (electron-builder packaging tooling), not shipped in the app",
    "GHSA-vxpw-j846-p89q": "undici: build-only (electron-builder packaging tooling), not shipped in the app",
    "GHSA-p88m-4jfj-68fv": "undici: build-only (electron-builder packaging tooling), not shipped in the app"
  },
  "pnpm": {
    "auditConfig": {
      "ignoreGhsas": [
        "GHSA-hmw2-7cc7-3qxx",
        "GHSA-vxpw-j846-p89q",
        "GHSA-p88m-4jfj-68fv"
      ]
    }
  }
}
